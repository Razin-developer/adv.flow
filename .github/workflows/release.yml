name: "Build and Release"

on:
  push:
    branches:
      - main
    tags:
      - 'v*'
  workflow_dispatch:

jobs:
  release:
    permissions:
      contents: write
    strategy:
      fail-fast: false
      matrix:
        platform: [macos-latest, ubuntu-22.04, windows-latest]

    runs-on: ${{ matrix.platform }}
    steps:
      - uses: actions/checkout@v4

      - name: setup node
        uses: actions/setup-node@v4
        with:
          node-version: lts/*

      - name: install Rust stable
        uses: dtolnay/rust-toolchain@stable

      - name: install dependencies (ubuntu only)
        if: matrix.platform == 'ubuntu-22.04'
        run: |
          sudo apt-get update
          sudo apt-get install -y libwebkit2gtk-4.1-dev build-essential curl wget file libxdo-dev libx11-dev libssl-dev libayatana-appindicator3-dev librsvg2-dev


      - name: install frontend dependencies
        run: npm install
        working-directory: tauri

      - name: Build and Stage CLI Sidecar
        shell: bash
        run: |
          cd tauri/src-tauri

          mkdir -p binaries

          HOST_TRIPLE=$(rustc -vV | grep host | cut -d ' ' -f 2)
          echo "Host triple: $HOST_TRIPLE"

          # Create placeholder files first so Tauri build.rs does not fail
          if [[ "$RUNNER_OS" == "macOS" ]]; then
            touch binaries/advflow-cli-aarch64-apple-darwin
            touch binaries/advflow-cli-x86_64-apple-darwin
            chmod +x binaries/advflow-cli-*
          fi

          if [[ "$RUNNER_OS" == "Linux" ]]; then
            touch binaries/advflow-cli-x86_64-unknown-linux-gnu
            chmod +x binaries/advflow-cli-*
          fi

          if [[ "$RUNNER_OS" == "Windows" ]]; then
            touch binaries/advflow-cli-x86_64-pc-windows-msvc.exe
          fi

          # Now build actual CLI
          cargo build --release --bin advflow-cli

          # Replace placeholder with real binary
          if [[ "$RUNNER_OS" == "macOS" ]]; then
            cp target/release/advflow-cli binaries/advflow-cli-aarch64-apple-darwin
            cp target/release/advflow-cli binaries/advflow-cli-x86_64-apple-darwin
            chmod +x binaries/advflow-cli-*
          fi

          if [[ "$RUNNER_OS" == "Linux" ]]; then
            cp target/release/advflow-cli binaries/advflow-cli-x86_64-unknown-linux-gnu
            chmod +x binaries/advflow-cli-*
          fi

          if [[ "$RUNNER_OS" == "Windows" ]]; then
            cp target/release/advflow-cli.exe binaries/advflow-cli-x86_64-pc-windows-msvc.exe
          fi

          echo "Final binaries:"
          ls -la binaries


      - uses: tauri-apps/tauri-action@v0

        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          tagName: v__VERSION__
          releaseName: "AdvFlow v__VERSION__"
          releaseBody: "Automated release from GitHub Actions."
          releaseDraft: true
          prerelease: false
          projectPath: tauri
          includeRelease: true

      - name: Build CLI Installer (Windows only)
        if: matrix.platform == 'windows-latest'
        shell: pwsh
        run: |
          cd tauri/src-tauri/windows
          if (!(Test-Path "C:\Program Files (x86)\NSIS\makensis.exe")) {
            choco install nsis -y
          }
          & "C:\Program Files (x86)\NSIS\makensis.exe" cli-installer.nsi
          Move-Item AdvFlow-CLI-Setup.exe ../../../AdvFlow-CLI-Setup.exe

      - name: Upload Standalone CLI Artifacts
        shell: bash
        run: |
          BIN_EXT=""
          if [[ "${{ matrix.platform }}" == "windows-latest" ]]; then BIN_EXT=".exe"; fi
          # The binary is built by tauri-action's beforeBuildCommand
          cp tauri/src-tauri/target/release/advflow-cli${BIN_EXT} ./advflow-cli-${{ matrix.platform }}${BIN_EXT}
          
      - name: Upload Extra Assets to Release
        uses: softprops/action-gh-release@v2
        if: startsWith(github.ref, 'refs/tags/') || github.event_name == 'workflow_dispatch'
        with:
          files: |
            AdvFlow-CLI-Setup.exe
            advflow-cli-*
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

